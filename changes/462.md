## Fixed

- Bounded coordination handoff listings with explicit pagination metadata while keeping older history reachable through limit and offset.

## Breaking changes

- Coordination handoff list responses now return a pagination envelope containing `handoffs`, `limit`, `offset`, and `truncated` instead of a bare array.
