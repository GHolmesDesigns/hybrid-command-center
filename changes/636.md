# C227 — Signal filter parity

- Extract the shared Status/Tasks multi-select filter control for Kanban, Tasks, and Signal.
- Add Escape and outside-click dismissal with focus returning to the filter summary.
- Present Signal Client, Project, and Campaign as responsive multi-select filters while keeping
  post-copy search transient and the three filter dimensions URL-durable.
- Keep Unbound client and No campaign available even when there are no named options.
