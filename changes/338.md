## Added

- Coordination MCP tools (C111): stdio tools for list/get/post/claim/complete/cancel/note on the
  agent handoff queue, plus `hcc://coordination/inbox?state=open`, with required `agent_label` on
  writes, a 10/minute coordination write budget, `mcp_agent_events` audit, and redacted tool
  results — no publish, Drive, or import side effects.

## Breaking changes

None.
