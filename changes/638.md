### Fixed

- Stabilize two intermittent e2e specs: wait for font-settled layout before Settings column geometry is measured, and wait for merge preview reads to finish before confirming import-identity merge choices.

### Changed

- Add `e2e/README.md` documenting per-spec data isolation and why the suite keeps `workers: 1` and `retries: 0`.

### Breaking changes

None.
