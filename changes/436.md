# Added

- MCP connection diagnostics now show the endpoint base URL and the SQLite store ID used by the
  connection. A disconnected Drive Files result includes the same evidence, so operators can
  reconnect Drive on the store the agent actually consulted.

## Breaking changes

None.
