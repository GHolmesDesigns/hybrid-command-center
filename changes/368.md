## Added

- Added server-bound MCP agent identities with scoped, independently expiring and revocable credentials, plus a Settings panel that displays each secret only once.

## Fixed

- Refused and audited attempts to impersonate another MCP agent by changing `x-agent-label`.

## Breaking changes

None. Existing operator-session MCP bearers remain supported.
