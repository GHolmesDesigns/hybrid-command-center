## Added

- Package an AWS EC2 production runtime with same-origin Caddy proxying, persistent SQLite volume wiring, deliberate migrations, single-writer enforcement, health checks, and fail-closed startup validation.
- Document a staging verification path for restart persistence, readiness, graceful shutdown, responsive layouts, and secret-free artifacts.

## Breaking changes

None.
