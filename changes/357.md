## Added

- The operating manual is now part of the repository at `docs/manual/`, so it is versioned and
  reviewed alongside the behaviour it describes. It covers the 5.4.0 MCP surface for all three
  audiences: what the coordination tools can and cannot reach, what is recorded when an agent acts,
  how to configure a local stdio or hosted HTTPS connection, and the field limits, refusal cases,
  and inbox resource form an agent needs to work the handoff queue.

## Breaking changes

None.
