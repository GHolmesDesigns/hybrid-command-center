## Added

- The browser tab now shows a bundled Hybrid Command Center icon. The SVG lives in the app's own
  static assets and is declared from `client/index.html`, so development and the production build
  both serve it locally. Sidebar logo branding in Settings stays separate — this is not a
  user-uploaded or remote image, and it does not change CSP.

## Breaking changes

None.
