## Fixed

- The quality gates pass on `main` again. A test file had drifted out of Prettier style, which
  failed the formatting step on every branch cut from `main` as well as on `main` itself. The change
  is a line break; no test, assertion, or application behaviour changed.

## Breaking changes

- None. Nothing in the running application changed; this is a contributor workflow change.
