# Changed

- MCP credential failures now return an authentication-shaped diagnostic that identifies a
  credential issued under an earlier `SESSION_SECRET` as a possible cause instead of appearing
  to be an unreachable server. Session-secret rotation and the required MCP credential reissue
  procedure are documented.

## Breaking changes

None.
