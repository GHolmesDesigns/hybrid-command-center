## Added

- `docs/publishing-integration.md` §17 — the record of what the publishing integration deliberately
  does not build. Every declined item carries the reason it was declined and the concrete thing that
  would reopen it, dated against the provider contracts they were read from, so a capability that is
  absent on purpose can be told apart from one nobody has got to yet.
- Named in that record: agent-driven posting through a provider's own MCP server and the
  Signal-over-MCP shape that would preserve the confirmation path; the post-level and platform
  configuration fields that exist in the contract and are never sent; the two provider platforms with
  no Signal channel; the media roles and AI-disclosure fields the live probe could not settle and why
  the account cap rather than the capability is the obstacle; mixed media sources in one post; the
  endpoints no caller needs; the absence of webhooks on either provider; and the figures gap a second
  provider would not close.

## Changed

- None. Nothing in the app's behaviour, schema, capability table, or provider handling changes.

## Breaking changes

None.
