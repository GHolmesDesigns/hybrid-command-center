## Added

- Added provider-qualified publishing account identities and per-target remote post identities so opaque Buffer channel and post IDs can be stored without changing existing Post Bridge history.
- Partitioned provider inventory and publishing reconciliation by provider to prevent one provider's rows from reaching another provider's adapter.

## Breaking changes

None.
