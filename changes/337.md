## Added

- Agent handoff queue (C110): durable `OPEN` → `CLAIMED` → `COMPLETED` | `CANCELLED` records with
  directed and open-pool claim rules, append-only notes, optional post idempotency, and operator
  HTTP cancel — coordination metadata beside the workspace, with no publish/Drive/import side
  effects and no `integration_events` row.

## Breaking changes

None.
