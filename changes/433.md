### Added
- Added credential-specific MCP verification that stays pending until the replacement credential makes a successful target-client call, and shows the credential ID and store ID without exposing token material.

### Changed
- Relabeled the operator-only MCP diagnostic as server health so it cannot be mistaken for client connection proof.

### Breaking changes
None.
