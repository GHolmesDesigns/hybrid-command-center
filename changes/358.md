## Added

- A tracked planning report at `docs/desktop-exe-transition-report.md` evaluating a Windows desktop
  package, revised against 5.4.0. It records that desktop is **not an approved direction** —
  `docs/cloud-hosting.md` remains the decided one — and states the two decisions that must precede
  any desktop work: whether desktop replaces, joins, or defers to the cloud track, and whether MCP
  exists on a desktop build at all.

## Breaking changes

None.
