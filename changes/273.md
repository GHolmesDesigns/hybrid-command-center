### Added

- Signal import dry runs resolve Drive media through the read-only metadata path, show each file's
  name, type, size, and resolution time in the preview, and write that fingerprint on confirm —
  refusing when a file changed between preview and confirm.

### Breaking changes

None.
