## Fixed

- **Show preview** succeeds on the first press from a freshly opened Signal editor. Post Bridge and
  Buffer account listings load in parallel and fail independently, so a cold Post Bridge listing
  failure no longer aborts a Buffer preview, overlapping refreshes share one Buffer walk, and a
  second click while the first request is in flight does not start a duplicate preview.

## Breaking changes

None.
