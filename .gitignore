node_modules/
.npm-cache/
dist/
data/*.db*
data/backups/
# Interrupted-write artifacts from FUSE-backed filesystems, not app data.
data/.fuse_hidden*
.env
.env.local
credentials.json
tokens.json
.claude/settings.local.json
.claude/scheduled_tasks.lock
.cursor/mcp.json.portable-backup
playwright-report/
test-results/
coverage/
# Linked Git worktrees. Checkouts of this repository, not content of it — committing one would
# commit a second copy of the tree. Both spellings: the plain one and the path the agent tooling
# actually uses.
.worktrees/
.claude/worktrees/
*.log
.DS_Store
